{
  "name": "setup-liquibase",
  "version": "2.0.0",
  "description": "Set up your GitHub Actions workflow with a specific version of Liquibase",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/main.ts -o dist --source-map --license licenses.txt",
    "test": "vitest run --pool=forks",
    "test:ci": "vitest run --pool=forks --no-coverage",
    "test:memory": "NODE_OPTIONS='--max-old-space-size=4096' vitest run --pool=forks",
    "test:coverage": "vitest run --pool=forks --coverage",
    "test:debug": "vitest run --pool=forks --reporter=verbose",
    "lint": "eslint src/**/*.ts",
    "format": "prettier --write src/**/*.ts",
    "package": "npm run build && npm test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/liquibase/setup-liquibase.git"
  },
  "keywords": [
    "actions",
    "liquibase",
    "setup",
    "database",
    "migration"
  ],
  "author": "Liquibase",
  "license": "FSL-1.1-ALv2",
  "dependencies": {
    "@actions/core": "^3.0.0",
    "@actions/exec": "^3.0.0",
    "@actions/http-client": "^4.0.0",
    "@actions/io": "^3.0.2",
    "@actions/tool-cache": "^4.0.0",
    "semver": "^7.7.4"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3.0.0",
    "@eslint/js": "^10.0.0",
    "@types/node": "^25.5.0",
    "@types/semver": "^7.7.1",
    "@typescript-eslint/eslint-plugin": "^8.57.1",
    "@typescript-eslint/parser": "^8.34.1",
    "@vercel/ncc": "^0.38.4",
    "eslint": "^10.0.3",
    "prettier": "^3.8.1",
    "typescript": "^5.9.3",
    "vitest": "^3.0.0",
    "@vitest/coverage-v8": "^3.0.0"
  },
  "overrides": {
    "inflight": "npm:@isaacs/inflight@^1.0.6",
    "glob": "^10.0.0",
    "rimraf": "^5.0.0",
    "flatted": ">=3.4.0"
  }
}
